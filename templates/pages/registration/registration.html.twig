{% extends 'pages/default/layout.html.twig' %}

{% block content %}
    <!--register form -->
    <div>
        <div class="main-overlay"></div>
        <div class="main-register-holder">
            <div class="main-register fl-wrap">
                <div class="close-reg"><i class="fa fa-times"></i></div>
                <h3>Sign In <span>Bands N'<strong> Gigs</strong></span></h3>
                <div class="soc-log fl-wrap">
                    <p>For faster login or register use your social account.</p>
                    <a href="#" class="facebook-log"><i class="fa fa-facebook-official"></i>Register with Facebook</a>
                </div>
                <div class="log-separator fl-wrap"><span>or</span></div>


                <div id="tabs-container">
                    <div class="tab">
                        <div class="tab">
                            <div id="tab-1" class="tab-content">
                                <div class="custom-form">
                                    {{ form_start(form) }}
                                    {{ form_row(form.eMail, {'label' : 'E-mail' }) }}
                                    {{ form_row(form.password.first, {'label' : 'Mot de passe' }) }}
                                    {{ form_row(form.password.second, {'label' : 'Retapez le mot de passe' }) }}
                                    <button type="submit" class="log-submit-btn"><span>Register</span></button>
                                    {{ form_end(form) }}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!--register form end -->
{% endblock %}
