{% extends "pages/default/layout.html.twig" %}


    {% block content %}
            <section class="gray-bg no-pading no-top-padding" id="sec1">
                <div class="col-list-wrap  center-col-list-wrap left-list">

                    <div class="container">

                        <!-- list-main-wrap-->
                        <div class="list-main-wrap fl-wrap card-listing">
                            <div class="container">
                                <div class="section-title">
                                    <h2>Manage events</h2>
                                    <div class="section-subtitle">Manage events</div>
                                    <span class="section-separator"></span>
                                    <p>Manage your events</p>
                                </div>
                            </div>
                            {% if events is empty %}
                            <p>You have no event</p>
                                <a href="{{ path('event-new') }}" class="btn transparent-btn custom-scroll-link">Create yours <i class="fa fa-music"></i></a>

                            {% endif %}
                            {% include "pages/events/list.html.twig" %}
                        </div>
                        <!-- list-main-wrap end-->
                    </div>
                </div>
            </section>
    {% endblock %}

