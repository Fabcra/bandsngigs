<div class="intro-item fl-wrap">
    <h2>Vous cherchez des évènements ??</h2>
    <h3>Trouvez les évènements de vos groupes favoris</h3>
</div>
<form class="filter" method="GET" action="{{ path('search') }}">


    <div class="main-search-input-wrap">
        <div class="main-search-input fl-wrap">
            <div class="main-search-input-item">
                <input type="text" placeholder="Band" name="by_band"/>
            </div>
            <div class="main-search-input-item location">
                <input type="text" placeholder="Location" name="by_location"/>
            </div>
            <div class="main-search-input-item">
                <select class="chosen-select" title="Style" name="by_style">
                    <option value="" disabled selected>Sélectionnez</option>
                    {% for s in styles %}
                        <option value="{{ s.style }}">{{ s.style }}</option>
                    {% endfor %}
                </select>
            </div>
            <button class="main-search-button"
                    onclick="window.location.href='listings-half-screen-map-list.html'">Recherche
            </button>
        </div>
    </div>
</form>
