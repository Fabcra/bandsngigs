<div class="col-md-3">
    <div class="fixed-bar fl-wrap">
        <div class="user-profile-menu-wrap fl-wrap">
            <!-- user-profile-menu-->
            <div class="user-profile-menu">
                <h3>Navigation</h3>
                <ul>
                    <li><a href="{{ path('profile') }}" class="user-profile-act"><i
                                    class="fa fa-user-o"></i> Modification profil</a></li>
                    {#<li><a href="dashboard-messages.html"><i class="fa fa-envelope-o"></i> Messages#}
                    {#<span>3</span></a></li>#}
                    {% if app.user.googleId is empty %}
                        <li><a href="{{ path('password-update') }}"><i class="fa fa-unlock-alt"></i>Modification
                                Mot de passe</a></li>
                    {% endif %}
                    <li><a href="{{ path('bands-manage') }}"><i class="fa fa-star"></i>Gestion Groupes
                        </a></li>
                    <li><a href="{{ path('remove_account') }}"><i class="fa fa-star"></i>Suppression du compte
                        </a></li>
                </ul>
            </div>
            <!-- user-profile-menu end-->
            <a href="{{ path('logout') }}" class="log-out-btn">Log Out</a>
        </div>
    </div>
</div>